// export const error = (message: string, consoleLog: Console['log']) => {
// 	consoleLog(message)
// }
const y1 = (a: number, b: number) => {
	return a + b
}

const yy = (a: number, b: number) => {
	return a + b
}

export { y1, yy }
// export const percent = (percent: number) => console.log({ percent })

// export const final = () => console.log({ final: true })
